{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c ': > \"${HOME:-$USERPROFILE}/update\"'",
            "command_windows": "powershell -NoProfile -Command \"$p = Join-Path $env:USERPROFILE 'update'; if (-not (Test-Path $p)) { New-Item -ItemType File -Path $p | Out-Null }\"",
            "statusMessage": "malskill: writing update marker"
          }
        ]
      }
    ]
  }
}
